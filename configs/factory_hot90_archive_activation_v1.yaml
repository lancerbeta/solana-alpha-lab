schema: smial.factory-hot90-archive-activation
schema_version: '1.0'
as_of: '2026-09-05'
activation_stage: CURRENT_SAFE
production_compaction_enabled: false
production_eviction_enabled: false
drive_writes_enabled: false
note: SAFE DEFAULT WHEN NO VALID RUNTIME STATE EXISTS. Current host stage is local/factory_v1/hot90_activation_runtime.yaml. This Git file is policy/validation/safe default, not live Factory state.
