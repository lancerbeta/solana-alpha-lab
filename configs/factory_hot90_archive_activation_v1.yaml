schema: smial.factory-hot90-archive-activation
schema_version: '1.0'
as_of: '2026-09-05'
activation_stage: CURRENT_SAFE
production_compaction_enabled: false
production_eviction_enabled: false
drive_writes_enabled: false
note: CURRENT_SAFE preserves live publisher SNAPPY + per-publication members and existing backup topology. Later commissioning may set WRITE_ONLY_SHADOW then DURABILITY_CUTOVER. RETENTION_ACTIVE is a separate destructive owner gate.
