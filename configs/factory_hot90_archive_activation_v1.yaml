schema: smial.factory-hot90-archive-activation
schema_version: '1.0'
as_of: '2026-09-05'
activation_stage: WRITE_ONLY_SHADOW
production_compaction_enabled: false
production_eviction_enabled: false
drive_writes_enabled: false
note: WRITE_ONLY_SHADOW uses ZSTD new writes and SNAPSHOT_PLUS_DELTA members. Drive writes, SQLite compaction, eviction, and backup durability cutover stay disabled until later owner-gated stages. RETENTION_ACTIVE is a separate destructive owner gate.
