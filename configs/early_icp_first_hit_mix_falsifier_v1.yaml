schema: smial.early-icp-first-hit-mix-falsifier
schema_version: '1.0'
atom_id: EARLY_ICP_FIRST_HIT_MIX_FALSIFIER_V1
capability_id: CAP-JUPITER-FREE-KEY-EARLY-ICP-FIRST-HIT-MIX-FALSIFIER-001
as_of: '2026-08-28'
wraps:
  select_eligible: src/solana_alpha_lab/factory/forward_h900_quote_capture.py
  quotes: src/solana_alpha_lab/factory/forward_h900_quote_capture.py
  scorer: src/solana_alpha_lab/factory/forward_mix_offline.py
  scorer_mutation: forbidden
external_authority:
  owner_phrase: 'OK EARLY_ICP_RETAINED_COHORT_MATURITY_CORRECTIVE_V1: one bounded Jupiter Free-key foreground falsifier using a local process-environment key only; this exact pre-registered EARLY_ICP_FIRST_HIT_MIX_FALSIFIER_V1 in-place corrective is authorized; any other experiment, promotion, Shadow, TWO_RUNG and /hypothesis-forge are forbidden; up to 20 quote-free density checks of Tokens V2 /recent plus one bulk /tokens/v2/search each, period 60s, pace >=3s; each /recent adds new pump.fun mints to a crash-safe retained candidate pool; each /search is built from that retained pool, not only the current /recent, max 100 mints, deterministic Y-blind order; frozen ICP, X, Y and age-band [300,600) are unchanged; the first search with valid_mix_eligible >=10 is the sole R0 snapshot, its search bytes are hash-bound, and a second /search is forbidden; stats5m mapping alone does not open quotes; quote only valid_mix_eligible then quote-only /swap/v2/order BUY at that R0 then quote-only SELL at absolute create_at+H900; after internal CAPTURE_COMPLETE, publish one immutable dataset bundle with commit-marker last and run existing unchanged score_frozen_mix_dataset; persist exactly one scientific terminal INVALID_EVIDENCE_REPLAN, CLOSE_EARLY_TAKER_VOLUME_MIX_FAMILY or EARN_ONE_CONFIRMATORY_FRESH_OOS and mark that Y consumed; x-api-key header only; no .env read, no key in URL/log/receipt/Git, no taker, /build, /execute, wallet, signer, transaction, paid plan, second provider, retry, fallback or mid-run owner intervention; cash cap $0; call cap 60 covering checks and quotes without raising the cap; quote_call_reserve 20; quoted = min(valid_mix_eligible, remaining/2, 29) and never below 10; unquoted valid-mix and structural invalid-X rows kept with typed Y-null; floors valid_mix_eligible >=10, decision-time mix_eligible >=10, rankable H900 >=8; typed missingness, no imputation, no universe deletion, UNKNOWN never zero; ICP-EARLY-PUMPFUN-V1 exclusions include the 2026-08-24 valuation window and the completed V2 STOP_BEFORE_QUOTES window; after 20 misses SLEEP_ELIGIBLE_BELOW_10 with no dataset, no score, no ResearchStore append and evidence_epoch unchanged; one Y-bearing window only on a new atom root; do not write or reopen V2 COMPLETE or prior SLEEP artifacts; X = R0_TAKER_VOLUME_MIX from stats5m buyVolume/(buyVolume+sellVolume) at that R0 only (dimensionless; no USD volume claim; no R1 search); no ln(R1/R0), no closed-family threshold, window, quartile, LOO or tau_b_floor reopen; Factory runner unchanged; Discovery, A7, Strategy, Bot, Shadow, alpha, NetReturn, micro-live, promotion and confirmatory second window forbidden.'
  credential_name: JUPITER_API_KEY
  credential_reads: 1
  dotenv_reads: false
  execute: false
  build: false
  taker: OMITTED_QUOTE_ONLY
  cash_cap_usd_cents: 0
  call_cap: 60
registry: configs/provider_route_capability_registry_v10.yaml
population:
  launchpad: pump.fun
  icp_id: ICP-EARLY-PUMPFUN-V1
  r0_age_band_seconds:
    min: 300
    max_exclusive: 600
  liquidity_usd_min: 1000
  search_one_call_max_mints: 100
  quote_pair_cap: 29
  min_eligible_before_quotes: 10
retained_cohort:
  search_from: retained_pool
  r0_floor: valid_mix_eligible
  search_pool_cap: 100
  scheduling_clock_for_unknown_created_at: first_seen_at
  eligibility_clock: hash_bound_firstPool_createdAt
consumed_mint_receipts:
  - docs/evidence/early_valuation_liquidity_divergence_confirmation/a1_runtime_receipt_v1.json
quote:
  slippage_bps: '100'
  notional_atomic: '10000000'
  horizon_seconds: 900
  horizon_kind: ABSOLUTE_CREATE_AT_PLUS_H900
  lateness_slack_seconds: 120
  buy_after: R0_SEARCH
  r1_search: forbidden
decision_rule:
  min_decision_time_eligible: 10
  min_rankable_h900: 8
  score_kind: SIGN_ONLY_KENDALL_TAU_B
  tau_b_floor: forbidden
  top_x_quartile: false
  leave_one_out_positive_share: forbidden
  scorer: score_frozen_mix_dataset
  scorer_mutation: forbidden
windows:
  max_windows: 1
  window_b: forbidden
  second_window: forbidden
  y_bearing_windows_max: 1
cadence:
  max_density_checks: 20
  density_check_period_seconds: 60
  quote_call_reserve: 20
  provider_pace_seconds: 3
execution_controls:
  retries: 0
  fallback: false
  persist_transaction_bytes: false
  provider_requests_max: 60
  min_interval_seconds: 3
  background_scheduler: false
  second_provider: false
  paid_plan: false
runtime_limits:
  timeout_seconds: 20.0
  max_response_bytes: 2000000
raw_retention:
  body: A4_OUTSIDE_GIT
  response_headers: SAFE_ALLOWLIST_ONLY
  journal: STAGING_OUTSIDE_ACTIVE_RDP
factory_runner: src/solana_alpha_lab/factory/runner.py
factory_runner_sha256: d8d22bcb51fb6992d40f09e58274c52e0f9942c12d043cc57b96ffca524e918f
non_claims:
  alpha: false
  netreturn: false
  execution: false
  strategy: false
  shadow: false
  discovery: false
  a7: false
  hypothesis_forge: false
  promotion: false
  two_rung: false
  confirmatory_second_window: false
  other_experiment: false
