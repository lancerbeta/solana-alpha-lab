---
schema: solana_alpha_lab.current_system_state
schema_version: "4.3"
status: VALIDATED_CANDIDATE_UI_ACTIVATION_PENDING
as_of: "2026-08-08"
manifest_version: "4.7"
roadmap_version: "4.7"
archive_version: "37.0"
active_task: TASK-27-A0-OFFLINE-FOUNDATION
contains_secrets: false
---

# CURRENT SYSTEM STATE — SOLANA MEMECOIN INTRADAY ALPHA LAB v4.3

## Truth boundary

Repository merge topology and GitHub CI are stronger implementation truth than
this Source candidate. This bundle cannot activate itself: Project Sources are
active only after the owner replaces the five mapped roles and reports the
exact seven-role smoke.

## Executive snapshot

    Control plane: TASK-27 A0 offline source reconciliation candidate
    Main: 082f3f8184e84c31c876a484cf8e876a40691f62
    Main CI: Repository validation run 31224401848, event push, SUCCESS
    A2: pool-interval price/volume research screen contract; synthetic only
    A3: historical collection authority contract; synthetic only
    A4: bounded public-history feasibility packet; synthetic only
    A5: five-role Source replacement candidate; UI activation pending
    Project Asset Catalog: unchanged by A2-A5
    Provider/API/RPC/WSS, raw history, R2/R3, wallet, signer, transaction and cash actions: 0
    Numeric modeled/observed NetReturn: forbidden

## Current flow

    TASK-26C offline canary readiness
      -> owner-packet review architecture, still no canary authority
      -> TASK-27 A0 offline history/volume data-contract foundation
      -> fresh seven-role Source smoke
      -> separate exact owner external-read review
      -> at most a bounded public-history feasibility capture, if separately approved
      -> independent Entry Gate before any research, strategy or execution inference

TASK-27 does not bypass the canary path. It contains no executable route, no
wallet and no economic result. It merely makes the future question "is a
limited public history usable enough to study?" testable without pretending
that a chart is fill, settlement or profit evidence.

## Current decision and limits

The candidate's next decision is source alignment, not provider access.
`ACTIVATION_CONFIRMED_USER_SMOKE` is required before A4 can support a separate
owner external-read review. That review remains a new authority boundary.

If a later external capture is approved, it may propose only one
GeckoTerminal public Solana pool OHLCV source candidate, no fallback provider,
at most six discovery reads and 24 OHLCV reads, 15-minute intervals, 24-hour
panels and at least twelve complete retained panels. These are future caps, not
permission and not evidence that the data exist.

`DESCRIPTIVE_ONLY` history is not `PIT_ADMISSIBLE`; a source-backed availability
proof would still be required. Missing values remain `UNKNOWN`, never zero,
flat, settled or a continuous path.

## Authority and health

`provider_read_authority=false`. No provider credential, R2/R3 value/path,
wallet, signer, transaction, funding, cash, deployment or strategy promotion
authority exists. The current Source candidate creates no new catalog record
and cannot change canonical state until a user UI replacement and smoke are
observed.

## Factory Fit and Product Horizon

Source alignment is the minimal NOW repair because it prevents a provider
request from outrunning owner-visible project memory. No generic synchronization
service is justified: one manifest-first bundle and one smoke are sufficient.

WATCH: a future capture closes the history route if it cannot supply the
bounded complete panels and evidence requirements. It must not broaden into
multi-provider collection, execution or alpha research merely to avoid a
negative result.

## Source activation handoff

Replace exactly five mutable roles from this candidate: canonical manifest,
roadmap, current system state, phase archive and active TASK-27 record. Keep
Operating System v8.5 and Blueprint v2.3 byte-for-byte. Run the provided
seven-role smoke. Until the result is returned, keep
`VALIDATED_CANDIDATE_UI_ACTIVATION_PENDING` and `STATE_CHANGE=NONE`.

## Changelog

- v4.3 — Adds repository-merged TASK-27 A2/A3/A4 offline contracts and the
  A5 Source replacement candidate. Main `082f3f8…` passed push CI run
  `31224401848`. No provider, raw data, wallet, signer, transaction, cash,
  strategy, PIT, PnL or NetReturn claim is added.
