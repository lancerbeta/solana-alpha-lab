# HORIZON_SCOPED_SCIENTIFIC_ELIGIBILITY_V1

Scientific N is now X300-valid `X_ELIGIBLE`. Outcome missingness is coverage
against that denominator and can only fail-close an experiment into the
existing data/science-option route.

Historical selection receipts stay byte-immutable
`FULL_LIFECYCLE_COMPLETENESS` caveats. They do not globally STOP
`START_NEW_SESSION`.

This atom does not run Forge and does not perform sealed C1 semantic
acceptance. That read-only acceptance is the next atom after merge.
