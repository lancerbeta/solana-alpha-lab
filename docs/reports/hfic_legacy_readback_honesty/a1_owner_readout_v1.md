# Честное чтение истории

История старых версий читается без падений. `diagnostics`: 17 сессий, readable 16, unresolved 1, provenance `CORRECTED_ORIGINAL_UNKNOWN`. 20 неизвестных времён — `EXPLORATORY_REUSE`. `prove-runtime` свежей сессии — `PROVEN` без флагов времени; метка хранилища `CORRECTED_ORIGINAL_UNKNOWN` остаётся отдельно. Конфликт идентичности — `proof_status=NOT_A_PROOF`, команда выходит с ненулевым кодом.

`FIRST_REAL_RUN_READINESS`: **GO** на `859271a8`. J3: freeze и awaiting — `RESUME_BASE`, runner-up — `FINISH_RUNNER_UP`, одна сессия и один слот, без `START_*`. J5: `NO_WORTHY_HYPOTHESIS`, `critic_launched=false`, forge-run `RESUME_V1`. J2: гейт на свежей копии `integrity=PASS`; чистый повтор — preflight `RETURN_EXISTING_SESSION`, forge-run `OWNER_CANDIDATE`, writes 0.

Не alpha. Эксперимент не запускался. История RDP не переписана. Реальный RDP не менялся.
