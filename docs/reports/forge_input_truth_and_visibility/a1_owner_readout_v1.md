# FORGE_INPUT_TRUTH_AND_VISIBILITY_V1 — Owner readout

Date: 2026-09-20 · Route: DIRECT_CURSOR_DELIVERY

Program: third atom of owner-path convergence. A4–A5 and the provenance
addendum are not this atom.

## What changed

One canonical `build_forge_input_receipt` builder now owns scientific
input/visibility. Material packet/vision visibility uses one pure
`evaluate_forge_packet_vision` source. On a commissioned Fast Lane plane the
builder assembles the same persist=False packet identity as slash
(`owner_focus`, evidence epoch, search key, commissioning status,
`research_memory_as_of`, START_NEW_SESSION selection caveat). Catalog
compact-all is only the uncommissioned fallback. A commissioned assembly miss
is `OBSERVABILITY_BLOCKED` / unknown, not catalog READY.
`forge_runnable=true` cannot precede a failing vision check. Ordinary
preflight machine-stops `OBSERVABILITY_BLOCKED` / vision failure before
synthesis. CONTROL `forge-control-ready` is an adapter over the same builder
plus operational extras. Historical C1 selection calibration stays a scoped
caveat against its own frozen inputs; a later C2 cumulative
`dataset_manifest_id` does not make that receipt `integrity_invalid`. Ordinary
packet selection keeps a protected LIVE CORPUS slot. The owner block always
shows `evidence_surface_mode` (`ordinary` when JSON is null) and historical
`caveat_router` when C1 integrity is PASS. `forge_input_next=STOP_BEFORE_SYNTHESIS`
is visibility, not slash halt: Prompt A only when preflight `action` is not
`STOP` and `forge_runnable` is true.

Owner classes are `FORGE_INPUT_READY`, `INPUT_NOT_READY`, and
`OBSERVABILITY_BLOCKED`. There is no `NO_WORTHY` on this surface.
`forge-input --no-write` never starts a session.

## Real C1/C2 no-write acceptance

persist=False against this machine's LIVE CORPUS instance.

- `forge_runnable=true`
- corpus_version **2**
- visible cohorts: `REL-20260902T111900Z-20260909T111900Z`,
  `REL-20260909T111900Z-20260916T111900Z`
- historical C1 `FULL_LIFECYCLE_COMPLETENESS` integrity **PASS**
  (`BLOCK_FORGE_SELECTION_RISK` remains a caveat, not a rewrite)
- `feature_grounding` / `packet_vision` **PASS** on the commissioned packet
  surface via `evaluate_forge_packet_vision` (PIT/missingness stay
  **NOT_EVALUATED** and are not a READY basis)
- `evidence_surface_mode` is present on the receipt (`null` for ordinary)
- owner `forge_input_next` when runnable: `STOP_BEFORE_SYNTHESIS` (not slash)
- writes: research_store 0, forge_context 0, session 0
- receipt_sha256 `1ca644284c7798eec4b17a172cbb4c38f23bf9c13937e368f4e97b1587d2a574`

Do not commit current runtime outputs or absolute machine paths.

## What did not change

No `/hypothesis-forge`, Prompt A/B/C, Independent Critic, statistical
diagnostic, V1 scientific probe, commissioning, historical receipt rewrite,
or new trial.

## Residual

Named consumer after merge/readback: A4 `FORGE_REPRESENTATION_LADDER_V1`.
`CAPABILITY_RADAR_NOW=NONE`. persist=False READY slash path does not
`rebuild_projection`, legacy-repair, or auto-commission. STOP before owner
merge phrase.
