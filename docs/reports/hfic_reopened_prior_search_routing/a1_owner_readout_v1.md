# Owner readout — HFIC_REOPENED_PRIOR_SEARCH_ROUTING_V1

## Terminal

Reopenable Git parks H11 and H13 were ledger-visible but absent from
search-memory, so Prompt A could rank other store IDs while never seeing those
prior bodies. This atom commissions canonical non-HFIC `HYPOTHESIS_VERSION`
records from current Git evidence and requires one-to-one
`ranked_prior_entries` in `FORGE_CONTEXT_PACKET`. Defective CONTROL
`HFIC-SESS-8F4A703030408365` stays immutable. Read-only active-RDP preview
reached `CONTROL_RECONSIDERATION_READY_AFTER_COMMISSION`. No live RDP write.
No new Forge slash. No merge in this atom.

## What landed

1. Generic reopenable inventory from `visible_as_prior_work=true` and
   `reopen_forbidden=false`. Current inventory is exactly H11
   `HYP-RC002-H11-LIFECYCLE-CLOCK-V1` and H13 `RC001-H13-COMPOSITE-VETO`.
2. Append-only, idempotent, conflict-detecting commissioning CLI
   `commission-reopened-priors --confirm-append-only`.
3. Prompt A body closure: `ranked_prior_entries` one-to-one with
   `ranked_prior_candidate_ids`. Silent ID-only priors fail closed as
   `RANKED_PRIOR_BODY_CONTEXT_INCOMPLETE` instead of shrinking ranked IDs.
4. Read-only `preview-reopened-prior-routing` reconstructs post-commission
   ranking, packet bytes, evidence epoch, memory eligibility, CONTROL
   `START_NEW_SESSION`, same C1 live corpus, and freeze compatibility.
5. Planned quarantine reuses existing memory policy for exactly
   `HFIC-SESS-8F4A703030408365` with
   `PRE_CAPABILITY_BASELINE_CALIBRATION_RESET`.

## Active-RDP preview (read-only)

- Current eligible count: 7. Planned eligible: 4.
- H11/H13 currently absent from search-memory; both reach the Prompt A set
  after planned overlay + exact-session quarantine.
- Packet 13404 bytes / 16384. Prior-memory 4 records / 2780 bytes.
- Old epoch `237ee709…` → planned `482ad503…`.
- Eligibility `b20f834c…` → `be64c8a3…`.
- Planned preflight action: `START_NEW_SESSION`.
- Freeze compat smoke: PASS. CONTROL trajectory-blind fence: intact.

## Explicit non-claims

- Not H11/H13 scientific PASS/FAIL. Historical BLOCKED_DATA stays historical.
- Not a new ranker, memory platform, database, or trajectory challenger.
- Not live RDP apply. Not a new CONTROL session. Not Critic. Not C2 import.
- Future HFIC-born hypotheses do not need this Git bridge.

## NEXT

Exact-head CI, then merge-readiness, then one owner merge phrase. After merge,
local preview → commission → exact-session quarantine apply → preview again →
`/hypothesis-forge CURRENT_REPRESENTATION_CONTROL`.
