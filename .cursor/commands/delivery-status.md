# Delivery status

For the exact task contract and existing `DELIVERY_HARNESS_V1` receipt, run
`scripts/delivery_harness.py check` and verify route continuity, Git head/tree,
dirty state, decision delta, validation owner, blockers and next action. Do not
infer a task or status from recency.
