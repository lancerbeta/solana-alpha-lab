# Delivery review

For the exact task contract and exact diff, verify `DELIVERY_HARNESS_V1` with
`scripts/delivery_harness.py check`. Always run code review; add goal/DoD,
architecture or refactor critics only on their triggers. If unavailable, record
`SINGLE_AGENT_REVIEW_FALLBACK` and apply the same deterministic gates.
