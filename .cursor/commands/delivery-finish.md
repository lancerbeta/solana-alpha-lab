# Delivery finish

For the exact task contract and candidate fingerprint under
`DELIVERY_HARNESS_V1`, run
`scripts/delivery_harness.py check`, proportional Factory Fit, Product Horizon,
capability radar, exact inventory and the elected single full gate. Prepare the
PR/read-back. Stop once for exact PR/head owner approval; only then evaluate the
guarded merge and verify exact `main` plus post-merge CI.
