# Delivery start

With Python 3.11 or newer, run the standard-library-only
`python -B scripts/delivery_harness.py check`, then build context for the exact
task contract under `DELIVERY_HARNESS_V1`. Do not install packages. Report
explicit gaps and the next safe action; do not infer current work from recency.
